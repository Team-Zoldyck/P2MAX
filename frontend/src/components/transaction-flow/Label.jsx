import React from "react";

const Label = ({ children }) => {
  return (
    <label className="text-[#05377F] mb-3 text-base font-normal">
      {children}
    </label>
  );
};

export default Label;
